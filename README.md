# MyProject

# The file WorldEcosystem.tif isn't available on GitHub because it was too heavy to push it here. So it's normal if there is an error of "no such file as WorldEcosystem.tif that exist" because it isn't uploaded 
